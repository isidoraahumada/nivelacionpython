import numpy as np 

a = np.array ([1,2,3,4]) #crea un arreglo 
b = np.array ([10,11,12,13]) #crea otro arreglo 

print a + b #suma cada elemento de los arreglos y los pone en uno nuevo (el primero elemento de a con el primer elemento de b, el segundo con el segundo, etc)
#a diferencia de las listas que cuando escribiamos esto une las dos listas, no las suma 
print a *b #multiplica cada elemento de los arreglos 
print a/b #divide cada elemnto de los arreglos
print a**b #a elevado a b para cada par de elementos  




